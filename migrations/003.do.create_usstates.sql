CREATE TABLE usstates (
  stateid INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  name TEXT NOT NULL
);

ALTER TABLE reports
  ADD COLUMN
    stateid INTEGER REFERENCES usstates(stateid) ON DELETE CASCADE NOT NULL;
